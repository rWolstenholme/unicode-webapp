extends layout

block head-extras
    link(rel='stylesheet', href='/stylesheets/search.css')

block content

    h1(style="padding:30px") You searched for #{char}
    a.tooltip(href="#")= char.name
        span
            ul
                each val,key in char
                    li= key + " ~ " + val